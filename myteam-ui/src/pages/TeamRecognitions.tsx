import React from 'react';
import { Navigate } from 'react-router-dom';

const TeamRecognitions: React.FC = () => {
  return <Navigate to="/recognitions" replace />;
};

export default TeamRecognitions;
